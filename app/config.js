module.exports = {
    app: {
        name: 'SocialSense',
        port: process.env.PORT || 3000,
        env: process.env.NODE_ENV || 'development'
    },
    db: {
        // إعدادات قاعدة البيانات
    },
    auth: {
        // إعدادات المصادقة
    }
}; 